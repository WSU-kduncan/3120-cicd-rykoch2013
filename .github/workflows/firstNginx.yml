name: Docker-First-Nginx

on:
  push:
    branches: [main]
    workflow-dispatch:
jobs:
  first-run:
      runs-on: ubuntu-latest
      steps:
        - run: echo "Starting"
        - uses: actions/checkout@v2
